name: bookish-env
channels:
  - conda-forge
  - defaults

dependencies:
  - python=3.11
  - pandas
  - numpy
  - scipy
  - requests
  - python-dotenv
  - pip
  - pip:
      - streamlit>=1.30.0
      - boto3
      - pyarrow>=18.0.0,<19.0.0
      - bcrypt
      - isbnlib
      - sodapy
      - pylint==4.0.4
      - pytest
      - build
