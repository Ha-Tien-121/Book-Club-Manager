"""
API layer (temporarily removed for CLI demo).

Reintroduce FastAPI here when restoring the HTTP API. Import service layer
functions (e.g. mark_book_as_read, get_recommendations) and keep business
logic in backend.recommender.service.
"""

# Example when restoring:
# from fastapi import FastAPI
# from backend.recommender.service import mark_book_as_read, get_recommendations
# app = FastAPI()
# @app.post("/users/{user_id}/mark-read") ...
# API endpoints intentionally omitted in this branch.
API_ENABLED = False
